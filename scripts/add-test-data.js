/**
 * SEED DATA ENGINE
 * This script was used for generating dummy test data.
 */

export async function seedTestData(userId) {
  return { success: true, message: "Seed data engine is currently disabled." };
}